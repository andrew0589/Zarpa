@* Shown when a signed-in user opens a page their account cannot use (admin only). *@

<PageTitle>Sin permiso — NavigationES</PageTitle>

<h1>Sin permiso</h1>
<p class="tagline">Tu cuenta no tiene acceso a esta página.</p>
<a class="btn-primary btn-link" href="/">Volver al inicio</a>
